<h1><%= @article.title %></h1>
<h1><%= @article.body %></h1>
<br />
<h3>Comments</h3>
<%= render partial: 'articles/comment', collection: @article.comments.reverse %>

<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Are you sure you would like to delete this article?"} %>
<%= link_to "Edit", edit_article_path(@article) %>
